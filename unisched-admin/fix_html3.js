const fs=require('fs'); let c=fs.readFileSync('index.html','utf8'); c=c.replace(/const uni = curUniversity\(\);\n  if \(uni\) \{\n    uni\.instructors = data;\n    App\.globalInstructors = Object\.values\(data\)\.map\(d => d\.name\)\.sort\(\);\n    buildInstrDatalist\(\);\n  \}/, 'App.globalInstructors = Object.values(data).map(d => d.name).sort();\n  buildInstrDatalist();'); fs.writeFileSync('index.html', c);
